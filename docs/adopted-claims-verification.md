# v3采用主张隔离验收

日期：2026-09-07。范围：调用层契约、装配、Skill/提示词、旧版兼容与同题迁移。不修改原书/原卡，不把真实书库或运行上下文纳入源码提交，不合并main。

## 实际变化

| 接缝 | 之前 | 现在 |
| --- | --- | --- |
| 采用声明 | 卡片级admit，局部采用只写在说明里 | v3每张采用卡显式adoption主张、归属、选定证据和未采用范围 |
| 见证主张 | 自动拷贝整卡claim/reasoning | 仅adopted_claim和Agent明确分析字段；不自动带入整卡条件/边界 |
| 原始内容 | 会话与答案同时携带 | 会话/书库完整保留；v3通过original_card_ref审计，不代表全卡采用 |
| 来源账本 | 所有采用卡的全部证据 | 仅所选证据；共享证据只关联真正选择它的卡 |
| 新旧调用 | 默认v2 | Skill默认v3；旧1/2和辅助工具默认2兼容，工具显式3不补adoption |
| 用户表达 | 原理详解、充分建议、圆桌、最多3轮澄清及续聊 | 完整保留，非固定字数/卡数；未用本改动压缩原理或关闭系统延伸 |

v3使用独立本地Schema，不联网解析。旧v2的15项提示词要求逐项保留；见tests/test_adoption_skill_contract.py。新原理的语义正确性仍须读原文，Schema不承担真实性证明。

## 测试与独立审核

基线：76 passed、63 subtests passed。核心先红测后实现；现有v1/v2完整包与本轮before实现作同输入对象对比，结果相同，原会话和知识对象深比较未变化。

新测试包含采用主张缺失/空白、空/重复/外卡证据、未选证据quote、逐字和长度限制、归属升级、共享证据关联、公开整卡字段注入、生成审计引用、旧版兼容、空采用保持旧约束、显式辅助装配及CLI、未知版本。旧JSON数值2.0版本兼容由验证器显式映射保留；辅助工具新参数约定整数2/3，不宣称支持浮点参数。

独立规格审阅已通过，额外构造10种知识组/论证/裁决/行动/学习/续聊/用户索引/审计引用错误输入，均拒绝。独立代码质量审核已给出Ready for PR；两组定向测试有重叠，不累计为新增用例。空白excluded_scope条目属于非阻断输入质量边角，空数组本身允许；语义审查不得把空说明当成充分论证。

## 同题同会话对照

使用已锁定的团队周会、学习迁移、读书会取舍三题；未重新选题、未重检索或改候选。当前Agent读取15张采用卡的全部会话证据，分别填写采用范围。旧题背景是模拟情境，不属于用户本人。

| 项目 | 团队周会 | 学习迁移 | 读书会取舍 |
| --- | --- | --- | --- |
| 候选/采用 | 30/5，保持 | 30/5，保持 | 30/5，保持 |
| 旧→新公开证据条数 | 18→10 | 13→8 | 17→8 |
| 结论、行动、知识组、圆桌、学习收获、续聊 | 相同 | 相同 | 相同 |
| 主文（不含来源账本） | 两处归属说明纠正 | 字节相同 | 字节相同 |

共11个问题与分析结构段逐对象相等；150个历史文件/原始输入指纹不变。48→26是所选证据的范围变化，**不是来源正确率提高，也不表示其余22条证据错误或已删除**。

团队题纠正：“实名便于追问”明确为系统沟通机制解释；书中直接主张仅写当前短引支持的避免匿名/量化评分、不挂钩升职加薪、与长期坦诚文化相联系。原稿中的改制细节不再作为当前短引已证实的叙述。这一处同时修正草稿principle与阅读段落，主方案和质询不变，未冒称所有字段逐字不变。

独立效果审阅另指出，OKR段中的“汇聚进展”和“提供输入”的职责区分仍易被理解为书中直接内容；现仅补“据此，我对本题作出的系统解释是”，未删解释或改变方案。修改前稿另存，独立复核确认问题已消除，三题均可进入PR。结构包仍只有前述principle变更；这一后续修正仅在阅读稿。

作者登记缺口仍保留：某书合著署名未完整入库，阅读稿已披露，未写回书目。候选库获得检索机会不等于所有书为结论背书；同源研究也不计为独立交叉验证。

本次同会话装配、验证与差异报告约32.90秒，其中书库加载约31.51秒，不含Agent阅读、语义判断和写稿。逐阶段token不可得，未估算；不是全链路生成延迟或模型稳定性测试。

## 保留的失败记录与局限

- 对照脚本首次未建输出父目录，CLI写入保护拒绝；补建本轮输出目录后运行成功，没有改CLI保护或覆盖旧文件。
- 全目录pytest曾误收集本轮保存的旧测试快照而报模块重名；仅将该新备份目录改为隐藏目录，未改生产测试配置。随后重新跑原命令。
- 三题是契约迁移和表达保留对照，不是新模型独立生成、不是真人执行效果，也不是全库语义验收。未完成的原卡返工不由本PR自动完成。
- 提交仅源码、规则、自编测试和本说明；真实前后文案、采用明细、diff、原文与输入锁在本地忽略目录，不随PR上传。

最终工作区全套：**95 passed、130 subtests passed**；最后提交前复跑pytest报告4.01秒，进程计时约4.22秒。独立规格、代码质量与效果审阅均已通过各自范围。暂存文件包括新文件后再次执行`git diff --cached --check`，修正新提示词的末尾空行后通过。

独立暂存源码检出不带私有库。首次打包测试因缺少未提交的仓颉依赖失败；按既有开发指南实际拉取v2.5.0，确认commit为`44692125abcdb93eab7b0e7a5ecd6ccadf92dc6f`后重跑：**91 passed、4 skipped、109 subtests passed**，pytest报告3.27秒，进程计时约3.47秒。4项跳过均为原有私有本地效果材料检查；本机95项已覆盖它们。没有为干净检出临时关闭测试。复用现有Python依赖环境，因此不是从零安装依赖的跨平台验收。

本轮备份、独立源码检出与公开依赖均只留在本地；临时路径的`/var`与`/private/var`别名核验已改为比较真实路径，未修改生产路径规则。发布以Git提交、远端分支和PR的实际回执为准。
