# 本地开发工具

- `inventory_books.py`：读取本机配置，盘点书籍 Markdown 并计算来源指纹，输出到标准输出。
- `project_check.py`：检查项目结构、仓颉提交、示例引用和可选 JSON Schema 验证。
- `project_paths.py`：共享项目路径及配置读取，不写盘。

从任何工作目录运行均按脚本所属项目定位。无参数的基础命令不需要第三方 Python 包。
