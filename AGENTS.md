# 项目协作规则

- 中文沟通；用户明确要求的范围优先。默认不启用 OpenSpec。
- 先读 README.md 和 docs/architecture.md，区分已实现能力与规划能力。
- 项目初始化完成不等于单书 MVP 完成；真实全书蒸馏、多作者综合必须有对应验收证据。
- 原书只读。不得修改 `data/originals/` 指向的外部文件，不批量复制、删除或移动用户书库。
- 自定义逻辑放入 `src/` 或 `prompts/`；不直接修改 `vendor/cangjie/`。依赖变更更新锁定记录并复核适配。
- 正文是观点证据来源；知识卡片和图谱是派生物；检索索引可以重建，不能反向成为知识事实源。
- 书内引用、案例与反例必须辨别。跳过序言、推荐语和营销文案，不把第三方的话归为作者本人观点。
- 代码须有中文用途说明；每个函数说明参数含义。纯领域规则不得依赖 CLI、HTML 或 Agent 会话。
- 新增功能前在实施计划中记录职责、事实源、依赖、接口、变化隔离、迁移回退六项检查。
- 生成的 Skill、图谱和关系不得把推断写成原文；未知页码不填造；检索不到要说明覆盖不足。
- `configs/local.json`、真实原书、模型凭据、运行数据和缓存不提交，不写入分发包。
- 修改后验证受影响路径；不以模拟材料证明真实书籍质量，也不以格式校验证明语义准确。
- 未获得明确指令时，不发布、不上传原书、不配置付费服务、不安装到用户全局 Agent 目录。
